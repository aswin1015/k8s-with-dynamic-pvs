# pending-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: pending-pod
spec:
  containers:
  - name: busybox
    image: busybox
    resources:
      requests:
        memory: "10Gi"
        cpu: "4"
    command: ["sleep", "3600"]
